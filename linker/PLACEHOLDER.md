# Linker command file goes here

Copy the `.cmd` linker command file from the CodeWarrior example project
that matches your board's exact device variant (see README.md and
docs/SETUP.md step 5). `tools/Build.ps1` picks up the first `*.cmd` file
it finds in this directory — delete this placeholder once you've added
the real one.
